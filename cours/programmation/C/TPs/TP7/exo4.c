/*  
    1. Dans le main, créez une variable seatsAvailable représentant le nombre total de places 
       disponibles pour un événement. Créez un tableau dynamique reservations pour suivre le nombre 
       de places réservées par section (une case = une section).

    2. Écrivez une fonction bookSeat qui modifie à la fois seatsAvailable et le tableau reservations. 
       Elle vérifie s’il reste des places avant de permettre une réservation.

    3. Écrivez une fonction processBookings qui appelle bookSeat plusieurs fois pour simuler des 
       réservations.

    4. Dans le main, appelez processBookings, affichez le nombre total de places disponibles et les 
       réservations par section.
*/

void bookSeat(int *seatsAvailable, int* reservationsSections) 
{
    
}

int main() {
     ,0

}